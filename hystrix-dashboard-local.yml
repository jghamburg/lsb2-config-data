server:
  port: 8080
eureka:
  instance:
    hostname: lsb2-eureka-server
    port: 8080
  client:
    registerWithEureka: false
    fetchRegistry: false
    serviceUrl:
      defaultZone: http://lsb2-eureka-server
spring:
  cloud:
    config:
      uri: ${CONFIG_SERVER_URI:http://lsb2-config-server}
